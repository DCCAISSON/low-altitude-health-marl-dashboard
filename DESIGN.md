# 低空健康服务决策看板设计规范

## 1. 整体定位

页面采用公共政策研究看板的表达方式，突出数据、模型结果和空间关系，保持严谨、清晰和可读。发布页面沿用本地平台既有视觉体系，不另行改变信息结构。

## 2. 色彩

| 角色 | 变量 | 色值 | 用途 |
| --- | --- | --- | --- |
| 页面背景 | `--bg` | `#f4f7fb` | 页面底色 |
| 面板 | `--panel` | `#ffffff` | 卡片与表格 |
| 柔和面板 | `--panel-soft` | `#f8fbff` | 卡片渐变 |
| 正文 | `--text` | `#0f172a` | 标题和正文 |
| 次要文字 | `--muted` | `#5b708b` | 注释和说明 |
| 强调色 | `--accent` | `#2563eb` | 交互与选中状态 |
| 正向状态 | `--good` | `#15803d` | 正向结果 |
| 提醒状态 | `--warn` | `#b45309` | 运行提醒 |
| 异常状态 | `--bad` | `#b91c1c` | 异常和负向结果 |

## 3. 字体

中文优先使用微软雅黑，英文和数字使用 Arial。正文不小于 14px；页面标题为 34px，移动端为 27px；一级面板标题为 31px，移动端为 24px。

## 4. 间距与布局

以 4px 为基本单位。桌面端内容最大宽度为 1860px，主体卡片间距为 18px。1280px 以下主要面板改为单列，640px 以下控制项改为单列。

## 5. 组件

- 卡片：白色到浅蓝渐变背景，1px 浅蓝边框，20px 圆角。
- 状态板：用于展示数据加载和平台状态。
- 图表：使用内嵌 SVG，保持既有指标配色。
- 表格：长表允许横向滚动，不压缩字段内容。
- 控制台：保留完整参数编辑、情景预览、模型训练和报告导出功能。

## 6. 交互

回放、指标切换和情景结果切换保持可用。训练运行时仅暂时禁用重复提交按钮，并显示明确的运行状态。交互过渡控制在 180ms 左右，不增加装饰性动画。

## 7. 层次

页面采用浅色主题，以边框、轻微阴影和背景明度变化区分层次。
